<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AWLike.RegisterPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Entry Placeholder="Username" Text="{Binding registerUser.Username}"/>
                <Entry Placeholder="Email" Text="{Binding registerUser.Email}"/>
                <Entry Placeholder="Password" IsPassword="True" Text="{Binding registerUser.Password}"/>
                <Entry Placeholder="Confirm Password" IsPassword="True" Text="{Binding registerUser.ConfirmPassword}"/>

                <Button Text="Register" Command="{Binding RegisterCommand}"/>

            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>